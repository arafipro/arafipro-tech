---
title: sample01
draft: true
---

https://www.youtube.com/watch?v=CG-USxkH_Ho

## Case1

### 原文（英語）

`<app-description>`  
	I want to create a next.js app that show these over-time animations.  
	lets say we have data on 5 companies, the size of comp and year in JSON format.  
	Write code to animate this to show the growth of the companies over time,  
	always keep the biggest companies on the top,  
	use a dark theme with good constrast,  
	i am on windows 11 and vs code.  
`</app-description>`

`<instruction>`  
	Create all the needed files and save them in the correct directories please for my Next js app:  
`</instruction>`

Create a step-by-step plan for the npx and npm commands for my app into one codeblock in plan A. 
Plan B should just repeat the `<app-description>` and `<instruction>` from above please:

### 日本語訳

`<app-description>`  
next.jsのアプリで、時間経過に伴うアニメーションを表示したいです。  
たとえば、5つの会社のデータ、会社の規模、年をJSON形式で持っているとします。  
これをアニメーション化し、企業の成長を時系列で表示するコードを書いてください。  
私はmacとVS Codeを使用しています。  
`</app-description>`

`<instruction>`  
私のNext jsアプリに必要なファイルをすべて作成し、正しいディレクトリに保存してください：  
`</instruction>`

私のアプリのnpxとnpmコマンドのステップ・バイ・ステップの計画をプランAで1つのコードブロックに作成する。  
プランBは、上記の`<app-description>`と`<instruction>`を繰り返すだけにしてください：

## Case2

### 原文（英語）

`<app-describtion>`  
I want to create a next.js app that reads data from a .json file.  
JSON file contains 6 different names and gender.  
Create a feature that selects random names,  
and pairs them up in a smooth transition,  
each pair must be in different genders.  
This app is for picking random teams for a boardgame.  
I am on windows 11 and vs code.  
`</app-describtion>`

`<instruction>`  
Create all the needed files and save them in the correct directories please for my Next js app:  
</instruction>

Create a step-by-step plan for the the npx and npm commands for my app into one codeblock in plan A.  
Plan B should just repeat the `<app-describtion>` and `<instruction>` from above please:

### 日本語訳

`<app-describtion>`  
私は、.jsonファイルからデータを読み込むnext.jsアプリを作成したいです。  
JSONファイルには、6つの異なる名前と性別が含まれています。  
ランダムな名前を選択し、スムーズな遷移でペアにする機能を作成します。  
各ペアは異なる性別である必要があります。  
このアプリは、ボードゲームのランダムチームを選ぶためです。  
私はWindows 11とVS Codeを使用しています。  
`</app-describtion>`

`<instruction>`  
私のNext.jsアプリに必要なファイルを作成し、正しいディレクトリに保存してください。  
`</instruction>`

私のアプリのnpxとnpmコマンドのステップバイステッププランをプランAに1つのコードブロックで作成してください。  
プランBは、上記の`<app-describtion>`と`<instruction>`を繰り返してください。
